# exchange_analysis
This project analyses data available through the ENTSO-E API using multiple methods (flow tracing, pooling), in order to determine the import sources and export sinks in the European electricity market on a per bidding zone and per type basis, for each bidding zone in the network.
